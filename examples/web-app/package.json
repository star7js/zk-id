{
  "name": "@zk-id/example-web-app",
  "version": "0.4.2",
  "description": "Web application example demonstrating zk-id integration",
  "private": true,
  "scripts": {
    "prestart": "npm run build --workspace=@zk-id/sdk",
    "start": "ts-node src/server.ts",
    "predev": "npm run build --workspace=@zk-id/sdk",
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "test": "echo 'No tests for examples'"
  },
  "dependencies": {
    "@zk-id/core": "*",
    "@zk-id/issuer": "*",
    "@zk-id/sdk": "*",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.2",
    "mocha": "^12.0.0-beta-8",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0"
  }
}
