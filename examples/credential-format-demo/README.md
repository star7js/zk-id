# Credential Format Demo

This demo shows how to convert a zk-id credential into an external credential format and back.
It is intended for interoperability experimentation only and is not a production flow.

## Run

```bash
npm install
npm run demo
```

## Notes
- This demo is optional and isolated from the core protocol.
- Do not use this in production without a full security review.
