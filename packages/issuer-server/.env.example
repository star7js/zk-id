# Server Configuration
PORT=3001
NODE_ENV=development

# API Security
API_KEY=your-secure-api-key-here

# Issuer Configuration
ISSUER_NAME=Your Identity Service

# Ed25519 Keys (base64-encoded DER format)
# Generate with: npm run dev (first run will generate keys)
# Or use a KMS/HSM for production
ISSUER_PRIVATE_KEY=
ISSUER_PUBLIC_KEY=

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,https://your-app.com
