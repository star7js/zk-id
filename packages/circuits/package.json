{
  "name": "@zk-id/circuits",
  "version": "0.2.0",
  "description": "Zero-knowledge circuits for identity verification",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/star7js/zk-id.git",
    "directory": "packages/circuits"
  },
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/trusted-setup.sh",
    "test": "PATH=\"$HOME/.cargo/bin:$PATH\" mocha test/**/*.test.js --timeout 60000 --exit",
    "build": "echo 'Circuits use compile script — no tsc build needed'"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  },
  "devDependencies": {
    "snarkjs": "^0.7.6",
    "circom_tester": "^0.0.24",
    "mocha": "^10.3.0",
    "chai": "^4.4.1"
  },
  "files": [
    "src/*.circom",
    "build/*.wasm",
    "build/*.zkey",
    "build/verification_key.json"
  ]
}
